<{{tagName}} class="
	wp-block-group 
	{{ style.border.radius ? "rounded-lg" }} 
	{{ style.border.width ? "border" }}
	{% if backgroundColor %} has-{{backgroundColor}}-background-color has-background {% endif %}
">
{{ render_blocks(children)}}
</{{tagName}}>
